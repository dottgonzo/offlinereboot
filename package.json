{
  "name": "offlinereboot",
  "version": "0.0.1",
  "description": "reboot linux if offline",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "hw": "git add -A && git commit -m 'oki'&&npm version patch -m patch&&git add -A && git commit --amend -C HEAD&& git push && npm publish",
    "patch": "npm version patch -m patch&&git add -A && git commit --amend -C HEAD&& git push && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dottgonzo/offlinereboot.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dottgonzo/offlinereboot/issues"
  },
  "dependencies": {
    "hwrestart": "0.0.12",
    "json-add": "0.0.1",
    "promise-test-connection": "0.0.2",
    "timerdaemon": "0.0.2",
    "verbo": "0.0.9"
  }
}
